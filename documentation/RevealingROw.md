`RevealingROw` (component)
==========================

ReavealingRow enables a row to have revealed options
that appear, for instance, when editing it.

Props
-----

### `children`

type: `node`


### `revealedContent` (required)

type: `node`


### `showingOptions` (required)

type: `bool`
defaultValue: `false`

